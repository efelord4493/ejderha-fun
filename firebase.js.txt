import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
import {
  getDatabase,
  ref,
  set,
  onValue,
  push,
  onChildAdded
} from "https://www.gstatic.com/firebasejs/10.12.0/firebase-database.js";

const firebaseConfig = {
  apiKey: "AIzaSyAuOsJYOrXdXj_GBzvgzOLEpzBp8BKAVIU",
  authDomain: "ejderha-a7e47.firebaseapp.com",
  databaseURL: "https://ejderha-a7e47-default-rtdb.firebaseio.com",
  projectId: "ejderha-a7e47",
  appId: "1:985795860958:web:afe91b2976521bc99ee54f"
};

const app = initializeApp(firebaseConfig);
const db = getDatabase(app);

export function updatePlayer(id, data) {
  set(ref(db, "players/" + id), data);
}

export function sendMessage(msg) {
  push(ref(db, "chat"), msg);
}

export function listenChat(cb) {
  onChildAdded(ref(db, "chat"), snap => cb(snap.val()));
}

export function listenPlayers(cb) {
  onValue(ref(db, "players"), snap => cb(snap.val() || {}));
}
